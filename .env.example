PORT=3000
# Generate a real random secret before deploying, e.g.: openssl rand -hex 32
JWT_SECRET=replace_this_with_a_long_random_secret
# Optional: fixed admin password (otherwise a random one is generated at seed time
# and printed to db/credentials_TO_DISTRIBUTE.csv)
ADMIN_PASSWORD=
